%h1 Teams
%style
  select {width: auto !important; font-size: 14px !important;}
  input {width: auto !important; font-size: 14px !important;}
.maincontent#maincontent
  - if @teams.empty?
    %p No team was created.
  - else
    = form_tag("teams/delete_teams", method: "post") do
      %table.cool.display#teams
        %thead
          %tr
            %th
            %th Name
            %th Project
            %th Google Drive
            %th Team Members
            %th
        - @teams.each do |t|
          %tr{ href: team_path(t.id) }
            %td{style: 'text-align: center'}= check_box_tag 'team[]', t.id
            %td
              %a{ href: team_path(t.id) }= t.name
            %td= t.project.name
            %td
              %a{ href: "#{t.google_drive}", target: "_blank"} Google Drive
            %td
              - t.students.each do |s|
                %a{ href: student_path(s.id), style: "margin-right: 8px" }= s.full_name
            %td
              %a.wdn-icon-calendar{ href: team_calendar_path(t.id) } Calendar
              \/
              %a{ href: team_work_track_path(t.id) } Track
      - if instructor?
        %div{style:"float: left"}
          %span{ style: 'font-size: 25px; color: #979797' } &#8627;
          = submit_tag 'Delete', {style: 'font-size: 10px; background: rgb(224, 95, 84)', confirm: 'Are you sure you want to delete this team?'}
          %a.wdn-button.wdn-button.energetic{ href: "#" } Send Email