%h1 Users
%style
  select {width: auto !important; font-size: 14px !important; }
  input {width: auto !important; font-size: 14px !important;}

.maincontent#maincontent
  -if @users.empty?
    No User was created.
  -else
    =form_tag("users/manage-users", method: "post") do
      %table.display#users
        %thead
          %tr
            %th
            %th Username
            %th User Type
            %th
        -@users.each do |u|
          %tr
            %td{style: 'text-align: center; width: 5%'}= check_box_tag 'user[]', u.id
            %td= u.name
            %td= u.utype
            %td
              &#10146;
              =link_to "See Details", u
      %div{style:"float: left"}
        %span{style: 'font-size: 25px;'} &#8627;
        = select_tag 'utype', options_for_select([['Please Select...', ''], ['Instructor', 'instructor'], ['Student', 'student'], ['Sponsor', 'sponsor']]), {style: 'width: 20%'}
        = submit_tag 'assign', {style: 'font-size: 10px !important; background: rgb(79, 199, 146)'}
        = submit_tag 'delete', {style: 'font-size: 10px !important; background: rgb(224, 95, 84)', confirm: 'Are you confirm to delete?'}
