%h1 Sponsors

-#=flash.each do |key, value|
-#  = value
.maincontent#maincontent
  - if @sponsors.empty?
    %p No Sponsor was created.
  - else
    %table.zentable
      %tr
        %thead
          %th First Name
          %th Last Name
          %th Organization
          %th Email
          %th
      -@sponsors.each do |s|
        %tr
        %td= s.first_name
        %td= s.last_name
        %td= s.orgnization
        %td
          %a{:href => "mailto:" + s.email }= s.email
        %td
          %a.wdn-button.wdn-button-triad{ href: sponsor_path(s.id) } Show
